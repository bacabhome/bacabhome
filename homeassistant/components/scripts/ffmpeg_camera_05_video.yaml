camara_entrada_video:
  alias: Camara Entrada Video
  sequence:
  - service: camera.record
    data:
      entity_id: camera.camara_entrada
      filename: '/config/www/media/videos/entrada/{{ now().strftime("%Y%m%d-%H%M%S") }}.mp4'
      duration: 60
      lookback: 10
