# https://www.home-assistant.io/integrations/history_stats/

platform: history_stats
name: House Switches On Today
entity_id: group.all_switches
state: 'on'
type: time
start: '{{ now().replace(hour=0).replace(minute=0).replace(second=0) }}'
end: '{{ now() }}'
