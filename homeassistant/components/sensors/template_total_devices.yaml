# https://www.home-assistant.io/integrations/template/

platform: template
sensors:
  template_total_devices:
    entity_id: []
    friendly_name: Template Total Devices
    value_template: >
      {%- set domains = ['device_tracker'] -%}
      {%- for domain in domains -%}
        {%- for item in states[domain] -%}
         {% if loop.first %}
           {{loop.length}}
         {% endif %}
        {%- endfor -%}
      {%- endfor -%}
